apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "splunk-sli-provider.fullname" . }}-test-connection"
  labels:
    {{- include "splunk-sli-provider.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "splunk-sli-provider.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
